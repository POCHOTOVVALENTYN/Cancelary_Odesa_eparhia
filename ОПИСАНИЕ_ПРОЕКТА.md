# Описание проекта: Telegram-бот для Одесской Епархии

## Общее описание

Telegram-бот разработан для предоставления информации о священниках Одесской Епархии. Бот позволяет пользователям получать доступ к базе данных священников через удобный интерфейс Telegram.

## Архитектура системы

### Компоненты системы

1. **bot.py** - Главный модуль приложения
   - Инициализация бота
   - Регистрация обработчиков команд
   - Управление жизненным циклом бота

2. **database.py** - Модуль работы с базой данных
   - Класс `Database` для всех операций с БД
   - CRUD-операции (создание, чтение, обновление, удаление)
   - Поиск и фильтрация данных

3. **models.py** - Модели данных
   - Класс `Priest` - модель священника
   - Методы форматирования данных
   - Преобразование в различные форматы

4. **handlers.py** - Обработчики команд и сообщений
   - Обработка команд пользователей
   - Обработка текстовых сообщений
   - Обработка callback-запросов (inline-кнопки)

5. **config.py** - Конфигурация
   - Токен бота
   - ID администраторов
   - Настройки базы данных
   - Константы приложения

6. **utils.py** - Вспомогательные функции
   - Парсинг дат
   - Валидация данных
   - Проверка прав доступа
   - Форматирование сообщений

### Архитектура базы данных

#### Таблица: priests

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER PRIMARY KEY | Уникальный идентификатор |
| name | TEXT NOT NULL | Имя священника |
| surname | TEXT NOT NULL | Фамилия священника |
| birth_date | DATE | Дата рождения |
| birth_place | TEXT | Место рождения |
| status | TEXT NOT NULL | Статус (Протоиерей, Иерей, Диакон, Протодиакон) |
| ordination_date | DATE | Дата рукоположения |
| service_place | TEXT | Место служения |
| education | TEXT | Образование (место обучения) |
| last_reward | TEXT | Последняя награда |
| created_at | TIMESTAMP | Дата создания записи |
| updated_at | TIMESTAMP | Дата последнего обновления |

### Поток данных

```
Пользователь → Telegram → bot.py → handlers.py → database.py → SQLite
                                                      ↓
                                              models.py (форматирование)
                                                      ↓
                                              Ответ пользователю
```

## Дизайн интерфейса

### Команды бота

#### Для всех пользователей:

1. **/start** - Приветствие
   - Показывает приветственное сообщение
   - Краткая инструкция по использованию

2. **/help** - Справка
   - Полный список команд
   - Примеры использования

3. **/search <запрос>** - Поиск
   - Поиск по имени или фамилии
   - Нечувствителен к регистру
   - Поддержка частичного совпадения

4. **/list** - Список всех
   - Пагинация результатов
   - Навигация через inline-кнопки
   - Показывает статус и имя

5. **/status <статус>** - Фильтр по статусу
   - Фильтрация по статусу священника
   - Показывает список с местами служения

#### Для администраторов:

6. **/add** - Добавление (в разработке)
   - Интерактивное добавление нового священника

7. **/edit** - Редактирование (в разработке)
   - Изменение существующих записей

8. **/delete** - Удаление (в разработке)
   - Удаление записей из базы данных

### Формат вывода информации

При отображении информации о священнике используется следующий формат:

```
👤 Имя Фамилия

Статус: Протоиерей
Дата рождения: 01.01.1980
Место рождения: Одесса
Дата рукоположения: 15.05.2005
Место служения: Свято-Успенский кафедральный собор
Образование: Одесская духовная семинария
Последняя награда: Наперсный крест
```

## Логика работы

### Поиск

1. Пользователь отправляет команду `/search` с запросом
2. Запрос обрабатывается в `handlers.search_command()`
3. Вызывается `database.search_priests(query)`
4. Выполняется SQL-запрос с LIKE для поиска
5. Результаты форматируются и отправляются пользователю

### Список

1. Пользователь отправляет `/list`
2. Загружается страница данных из БД (пагинация)
3. Формируется сообщение со списком
4. Создаются inline-кнопки для навигации
5. При нажатии кнопки обрабатывается callback и загружается следующая страница

### Фильтр по статусу

1. Пользователь отправляет `/status <статус>`
2. Статус валидируется и нормализуется
3. Выполняется запрос к БД с фильтром по статусу
4. Результаты форматируются и отправляются

### Безопасность

- Проверка прав администратора перед выполнением административных команд
- Валидация всех входных данных
- Использование параметризованных SQL-запросов (защита от SQL-инъекций)
- Ограничение длины сообщений

## Технические детали

### Используемые библиотеки

- **python-telegram-bot** (версия 20.7)
  - Официальная библиотека для работы с Telegram Bot API
  - Поддержка асинхронных операций
  - Обработка команд, сообщений, callback-запросов

### База данных

- **SQLite** - встроенная база данных
  - Не требует отдельного сервера
  - Легковесная и быстрая
  - Идеально для небольших и средних проектов
  - Автоматическое создание файла БД

### Обработка ошибок

- Логирование всех операций
- Graceful handling ошибок базы данных
- Информативные сообщения об ошибках для пользователей

### Производительность

- Пагинация для больших списков
- Ограничение количества результатов в поиске
- Эффективные SQL-запросы с индексами

## Расширяемость

### Возможные улучшения

1. **Интерактивное добавление данных**
   - Многошаговый диалог для добавления священника
   - Валидация на каждом шаге

2. **Экспорт данных**
   - Экспорт в CSV/Excel
   - Генерация отчетов

3. **Расширенный поиск**
   - Поиск по месту служения
   - Поиск по дате рукоположения
   - Комбинированные фильтры

4. **Статистика**
   - Количество священников по статусам
   - Графики и диаграммы

5. **Мультиязычность**
   - Поддержка украинского языка
   - Переключение языков

6. **Интеграция с внешними системами**
   - Импорт из Excel/CSV
   - Синхронизация с другими базами данных

## Структура проекта

```
Cancellary_Bot/
├── bot.py                    # Главный файл
├── database.py               # Работа с БД
├── models.py                 # Модели данных
├── handlers.py               # Обработчики
├── config.py                 # Конфигурация
├── utils.py                  # Утилиты
├── add_sample_data.py        # Скрипт для тестовых данных
├── requirements.txt          # Зависимости
├── .gitignore               # Игнорируемые файлы
├── README.md                # Основная документация
├── ARCHITECTURE.md          # Архитектура
├── ИНСТРУКЦИЯ.md            # Подробная инструкция
└── ОПИСАНИЕ_ПРОЕКТА.md      # Этот файл
```

## Заключение

Проект представляет собой полнофункциональный Telegram-бот для управления информацией о священниках Одесской Епархии. Архитектура спроектирована с учетом расширяемости и поддерживаемости. Код структурирован, документирован и готов к использованию.

Все компоненты написаны на русском языке, включая интерфейс пользователя, комментарии в коде и документацию.
