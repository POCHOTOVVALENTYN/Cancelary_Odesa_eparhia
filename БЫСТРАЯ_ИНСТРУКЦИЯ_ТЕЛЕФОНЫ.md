# 📞 Быстрая инструкция: Импорт телефонов на сервер

## ✅ Что было сделано

1. ✅ Добавлено поле `phone` в модель `Priest`
2. ✅ Обновлена база данных с автоматической миграцией
3. ✅ Создан парсер `kliriki_parser.py` для обработки файла
4. ✅ Создан скрипт импорта `import_phones_from_kliriki.py`
5. ✅ Телефоны теперь отображаются в меню:
   - 📋 **Список** - все священники с телефонами
   - 🎉 **Именинники** - именинники с телефонами
   - 🔍 **Поиск** - поиск с отображением телефонов
6. ✅ Все изменения закоммичены и отправлены на GitHub

## 🚀 Команды для развертывания на сервере

### Шаг 1: Подключение к серверу

```bash
ssh -i ~/.ssh/ваш_ключ.key opc@ВАШ_ПУБЛИЧНЫЙ_IP
```

### Шаг 2: Обновление кода

```bash
cd /home/opc/telegram-bot
git pull origin main
source venv/bin/activate
```

### Шаг 3: Тест чтения файла (опционально)

```bash
python3 test_kliriki_read.py
```

**Ожидаемый результат:**
```
✅ Файл найден
✅ pandas импортирован
✅ openpyxl импортирован
✅ Файл прочитан!
   Строк: 150
   Столбцов: 10
```

### Шаг 4: Импорт телефонов

```bash
python3 import_phones_from_kliriki.py
```

**Ожидаемый результат:**
```
📂 Загрузка файла: data/kliriki.xlsx
✅ Извлечено записей: 150
✅ Загружено священников из БД: 200
✅ Обновлен телефон для: ИВАНОВ Иван Иванович -> +380501234567
...
📊 СТАТИСТИКА ИМПОРТА
Всего записей: 150
Найдено совпадений: 140
Обновлено телефонов: 138
```

### Шаг 5: Копирование в /opt и перезапуск

```bash
sudo rsync -av --exclude='venv' --exclude='.git' --exclude='__pycache__' \
  /home/opc/telegram-bot/ /opt/telegram-bot/

sudo systemctl restart telegram-bot
sudo systemctl status telegram-bot
```

### Шаг 6: Проверка логов

```bash
sudo journalctl -u telegram-bot -f
```

**Нажмите Ctrl+C для выхода**

## 🧪 Проверка в Telegram

1. Откройте бота в Telegram
2. Нажмите **📋 Список** - должны отображаться телефоны
3. Нажмите **🎉 Именинники** → выберите категорию → проверьте наличие телефонов
4. Нажмите **🔍 Поиск** → введите фамилию → проверьте телефон в результате

## 📱 Формат отображения

```
АРАБАДЖИ Дмитрий Олегович
Сан: Протоиерей
🎂 Возраст: 34 лет
📞 Телефон: +380501234567
📍 Место служения: г. Одесса Свято-Успенский кафедральный собор
```

## ⚠️ Устранение проблем

### Проблема: "No module named 'openpyxl'"

```bash
source venv/bin/activate
pip install -r requirements.txt
```

### Проблема: "Файл не найден"

```bash
ls -la data/kliriki.xlsx
# Если файла нет, выполните git pull еще раз
```

### Проблема: Бот не показывает телефоны

```bash
# Убедитесь, что код скопирован в /opt
sudo rsync -av --exclude='venv' --exclude='.git' --exclude='__pycache__' \
  /home/opc/telegram-bot/ /opt/telegram-bot/

# Перезапустите бота
sudo systemctl restart telegram-bot
```

## 📚 Дополнительная информация

Полная инструкция: `ИНСТРУКЦИЯ_ИМПОРТ_ТЕЛЕФОНОВ.md`

---

**Время выполнения: ~5 минут**
